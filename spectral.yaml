rules:
  owasp-best-practices:
    description: "OWASP security best practices check"
    given: "$.components..securitySchemes[*]"
    severity: "warn"
    then:
      - field: "type"
        function: "truthy"
        functionOptions:
          truthy: ["apiKey", "http", "oauth2", "openIdConnect"]
