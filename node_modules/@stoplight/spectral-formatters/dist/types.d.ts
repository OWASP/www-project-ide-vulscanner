import { ISpectralDiagnostic, Ruleset } from '@stoplight/spectral-core';
import type { DiagnosticSeverity } from '@stoplight/types';
export declare type FormatterOptions = {
    failSeverity: DiagnosticSeverity;
};
export declare type FormatterContext = {
    ruleset: Ruleset;
    spectralVersion: string;
};
export declare type Formatter = (results: ISpectralDiagnostic[], options: FormatterOptions, ctx?: FormatterContext) => string;
