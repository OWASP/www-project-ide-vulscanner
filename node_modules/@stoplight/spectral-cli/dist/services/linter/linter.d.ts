import { IRuleResult, Ruleset } from '@stoplight/spectral-core';
import { ILintConfig } from '../config';
export interface LinterResult {
    results: IRuleResult[];
    resolvedRuleset: Ruleset;
}
export declare function lint(documents: Array<number | string>, flags: ILintConfig): Promise<LinterResult>;
