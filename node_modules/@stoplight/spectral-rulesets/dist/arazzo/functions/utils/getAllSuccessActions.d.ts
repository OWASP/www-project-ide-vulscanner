import { ArazzoSpecification, Workflow, Step, SuccessAction } from '../types/arazzoTypes';
export default function getAllSuccessActions(step: Step, workflow: Workflow, arazzoSpec: ArazzoSpecification): SuccessAction[];
